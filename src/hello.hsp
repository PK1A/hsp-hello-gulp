var HelloCtrl = require('./hello.js');

# template hello using ctrl:HelloCtrl
<h1>Hello, {ctrl.name}!</h1>
<input model="{ctrl.name}"><button onclick="{ctrl.clear()}">Clear</button>
# /template

module.exports = hello;